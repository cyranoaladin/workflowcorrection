NEXT_PUBLIC_API_BASE_URL=https://workflow.example.com/api
# For local development against a running backend, override with:
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
# Never add NEXT_PUBLIC_ADMIN_API_TOKEN here — the bearer token is injected by Caddy server-side.
